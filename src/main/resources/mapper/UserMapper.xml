<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.chat_server.mapper.UserMapper">

    <resultMap id="UserMap" type="com.example.chat_server.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="role" column="role"/>
    </resultMap>

    <select id="findByUsername" resultMap="UserMap">
        SELECT * FROM users WHERE username = #{username} LIMIT 1
    </select>

    <insert id="insert" parameterType="com.example.chat_server.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, role)
        VALUES (#{username}, #{password}, #{role})
    </insert>

</mapper>