<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.chat_server.mapper.MemberMapper">

    <!-- member_id 자동 생성 -->
    <insert id="insertMember"
            parameterType="com.example.chat_server.entity.Member"
            useGeneratedKeys="true"
            keyProperty="memberId">
        INSERT INTO member
            (dept_id, name, email, position, join_date, birth, state_id, phone_num)
        VALUES
            (#{deptId}, #{name}, #{email}, #{position}, #{joinDate}, #{birth}, #{stateId}, #{phoneNum})
    </insert>

    <select id="existsByEmail" resultType="int">
        SELECT COUNT(*)
        FROM member
        WHERE email = #{email}
    </select>

    <select id="existsByPhone" resultType="int">
        SELECT COUNT(*)
        FROM member
        WHERE phone_num = #{phoneNum}
    </select>

</mapper>
